#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>
/**
 * _strlen - returns length of string
 * @s: string measured
 * Return: standard library version
 */
int _strlen(char *s)
{
	return (strlen(s));
}
