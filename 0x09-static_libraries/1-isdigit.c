#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>
/**
 * _isdigit - checks for 0 to 9
 * @c: number checked
 * Return: standard library version
 */
int _isdigit(int c)
{
	return (isdigit(c));
}
