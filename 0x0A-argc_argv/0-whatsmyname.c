#include "main.h"
#include <stdio.h>
/**
 * main - prints program name
 * @argc: counts the arguments
 * @argv: vector
 * Return: 0
 */
int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);

	return (0);
}
