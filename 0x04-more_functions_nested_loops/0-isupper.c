#include "main.h"
/**
 * _isupper - determines case sensitivity
 *@c: character to check

 * Return: 1 if uppercase, 0 therwise
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
		Return(1);
	}
	else
	{
		Return(0);
	}
}
